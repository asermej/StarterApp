<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.3.xsd">

    <changeSet id="20251108004033-create-persona-categories-table" author="liquibase">
        <preConditions onFail="MARK_RAN">
            <not>
                <tableExists tableName="persona_categories"/>
            </not>
        </preConditions>
        <createTable tableName="persona_categories">
            <column name="id" type="UUID" defaultValueComputed="gen_random_uuid()">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="persona_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="category_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="timestamp" defaultValueComputed="CURRENT_TIMESTAMP">
                <constraints nullable="false"/>
            </column>
            <column name="updated_at" type="timestamp">
                <constraints nullable="true"/>
            </column>
        </createTable>

        <!-- Foreign key to personas table -->
        <addForeignKeyConstraint
            baseTableName="persona_categories"
            baseColumnNames="persona_id"
            constraintName="fk_persona_categories_persona"
            referencedTableName="personas"
            referencedColumnNames="id"
            onDelete="CASCADE"/>

        <!-- Foreign key to categories table -->
        <addForeignKeyConstraint
            baseTableName="persona_categories"
            baseColumnNames="category_id"
            constraintName="fk_persona_categories_category"
            referencedTableName="categories"
            referencedColumnNames="id"
            onDelete="CASCADE"/>

        <!-- Unique constraint on persona_id + category_id combination -->
        <addUniqueConstraint
            tableName="persona_categories"
            columnNames="persona_id, category_id"
            constraintName="uq_persona_categories_persona_category"/>

        <!-- Index on persona_id for lookup performance -->
        <createIndex tableName="persona_categories" indexName="idx_persona_categories_persona_id">
            <column name="persona_id"/>
        </createIndex>

        <!-- Index on category_id for lookup performance -->
        <createIndex tableName="persona_categories" indexName="idx_persona_categories_category_id">
            <column name="category_id"/>
        </createIndex>
    </changeSet>
</databaseChangeLog>

